# Puoi personalizzare i comandi PD qui.
CONFIG = {
    # Esempi:
    # "subfinder_cmd": ["subfinder", "-dL", "{in}", "-o", "{out}", "-all", "-recursive"],
    # "httpx_cmd": ["httpx", "-l", "{in}", "-o", "{out}", "-silent", "-follow-redirects", "-no-color", "-timeout", "10", "-status-code"],
    # "katana_cmd": ["katana", "-list", "{in}", "-o", "{out}", "-headless", "-js-crawl"],
    # "nuclei_cmd": ["nuclei", "-l", "{in}", "-jsonl", "-o", "{out}", "-c", "50", "-rate-limit", "100"],
}
